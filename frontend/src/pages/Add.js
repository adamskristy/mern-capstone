import RatingForm from "../components/RatingForm";

function Add({ user }) {
    return (
        <div>
            
            <RatingForm user={user} />
        </div>
    );
}

export default Add;